# Copy this to .env and fill in your values before running docker compose
# docker compose automatically loads .env from the same directory

# Flask security keys — change these to random strings in production!
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-to-a-random-32-char-string
JWT_SECRET_KEY=change-me-to-another-random-32-char-string
